import React from "react";
import Classes from "./Header.module.css";
import "./Header.module.css";

const Header = () => {
    return (
        <header>Picture generator on ASCII base</header>
    );
}

export default Header;